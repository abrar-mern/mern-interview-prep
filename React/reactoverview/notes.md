1.) What is React ?
Answer - React is a JS library that is used to create UI where we can use predefined functionalities. It is an component - [Reusable code]. It is an declartive approach.
It is a single based application approach.

-------------------------------------------------------------------------------------

2.) Uses of react ?
Answer - 
1.) Reusability
2.) Maintanability
3.) DRY (Do not repeat yourself)

-------------------------------------------------------------------------------------

3.) What is difference between package.json() and lock-package.json() ?
Answer - The major difference between package.json() and lock-package.json() is when we package.json list the project name and the range of versions and dependencies and the lock-package.json is when it was installed it locks the exact version

--------------------------------------------------------------------------------------