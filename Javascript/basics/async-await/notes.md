1.) What is an Async function ?
Answer - A async function will always return a promise if we don not return promise and if we return a variable so it automatically wraps the variable and return a promise

-----------------------------------------------------------------------

2.) Async and await ?
Answer - Async and await are used to handle the promises

-----------------------------------------------------------------------

3.) Await Keyword ?
Answer - Awiat is a keyword that can be used only inside the async function

-----------------------------------------------------------------------
