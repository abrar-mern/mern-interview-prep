1.) What are syntax error ?
Answer - These occured when code is not written accoding to the javascript syntax rules.
Eg :- let x =;

-------------------------------------------------------------------------------------

2.) What are reference error ?
Answer - These occures when we refer to any variables or methods that does not exist

Eg: - let val = y; //y does not exist
      function x(){
        data() // this method does not exist
      }

-------------------------------------------------------------------------------------

3.) What is type error ?
Answer - This error is occured when we perform operation on wrong data type 

-------------------------------------------------------------------------------------

4.) What is range error ?
Answer - The range error is if we are trying to access the value outside the range

-------------------------------------------------------------------------------------

5.) What is URL ?
Answer - So once it's trying to get a URI, we just check if my concept is defined in the URI. So the meaning is a URI is simply defined as a resource identifier and a URI is one of the uniform resource identifier and when we are passing URI, so there are the difference like we are doing a protocol that is we can get HTTP, HTTPS, or FTP as well. And if it is an HTTPS, then it will establish the SSL connection and then after that, it is a domain name that we call as a first domain between the IP of the domain as well. On that, the IP, we can use domain name and we can find the IP out of it. And then if you want to identify like what is the URI, which URI, URI after the all URIs are not URI, but all URIs are not URIs. And for the main concept is time to explain us if we want to encode that any URI and HTTP request and a domain and we will use encoder, encoders, encoding URI component and this encoding as you can have passwords or slash, and if a specific people are not that is not a type of URI and the new URI